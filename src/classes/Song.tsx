class Song {
    title: string;
    artist: string
    duration: string

    constructor(title: string, artist: string, duration : string) {
        this.title = title 
        this.artist = artist
        this.duration = duration
    }
}

export default Song;